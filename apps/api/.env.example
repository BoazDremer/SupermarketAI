# HTTP port for the NestJS API (default 3000).
PORT=3000

# Required when the API (or Prisma scripts) opens a DB connection.
# Copy from packages/db/.env.example or set the same value here.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/supermarket_price_compare?schema=public"
